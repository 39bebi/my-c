#include <stdio.h>

int main() {
   float length,width,area;
   printf("Enter a length");
   scanf("%f",&length);
   printf("Enter a width");
   scanf("%f",&width);
   area=length*width;
   printf("area of rectangle is %f",area);
}
